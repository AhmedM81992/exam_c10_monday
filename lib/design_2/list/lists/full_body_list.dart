import 'package:flutter/material.dart';

class FullBodyList extends StatelessWidget {
  const FullBodyList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
